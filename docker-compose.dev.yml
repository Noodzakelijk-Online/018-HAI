# Legacy companion file retained for scripts that pass both the default and
# development Compose filenames. The canonical stack already supplies safe
# loopback port bindings and source builds in docker-compose.local.yml.
services: {}
