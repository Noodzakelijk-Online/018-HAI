# Compatibility entrypoint for operators who run Compose from kafka/.
# The retired ZooKeeper/three-broker stack must not be started separately.
name: 018-hai

include:
  - path: ../docker-compose.local.yml
    env_file: ${HAI_ENV_FILE:-../.env.local}
