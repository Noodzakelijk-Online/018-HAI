# Canonical local HAI entrypoint. The implementation is kept in the included
# file so `docker compose up` cannot start the retired prebuilt-image stack.
name: 018-hai

include:
  - path: ./docker-compose.local.yml
