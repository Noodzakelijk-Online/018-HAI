# Compatibility entry point for `docker compose up`.
#
# The canonical Windows/local stack builds HAI from this checkout and keeps
# optional runtimes profile-gated. Keeping this small wrapper prevents the
# obsolete prebuilt-image and multi-broker configuration from being started by
# accident when a developer uses Docker Compose's default filename.
include:
  - path: ./docker-compose.local.yml
