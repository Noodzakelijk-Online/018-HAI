# Compatibility entrypoint for operators who run Compose from gate/.
# Gateway behavior is defined only by the canonical HAI topology.
name: 018-hai

include:
  - path: ../docker-compose.local.yml
    env_file: ${HAI_ENV_FILE:-../.env.local}
