# Compatibility entrypoint for operators who run Compose from backend/.
# HAI has one topology; this file delegates to the repository-level canonical
# source-built stack instead of starting the retired external backend image.
name: 018-hai

include:
  - path: ../docker-compose.local.yml
    env_file: ${HAI_ENV_FILE:-../.env.local}
