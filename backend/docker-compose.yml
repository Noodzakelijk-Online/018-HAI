# Compatibility entry point. The local HAI stack is source-built at the repository root.
# Use `docker compose --env-file .env.local -f docker-compose.local.yml up --build`
# for the documented command; this file keeps older backend-local invocations safe.
include:
  - path: ../docker-compose.local.yml
