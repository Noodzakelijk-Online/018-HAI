# Compatibility entry point. The backend is part of the canonical local HAI
# topology; do not start it with an obsolete standalone image/database stack.
include:
  - path: ../docker-compose.local.yml
