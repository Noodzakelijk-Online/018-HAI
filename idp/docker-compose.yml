# Compatibility entrypoint for operators who run Compose from idp/.
# The identity provider is part of the one canonical HAI topology.
name: 018-hai

include:
  - path: ../docker-compose.local.yml
    env_file: ${HAI_ENV_FILE:-../.env.local}
