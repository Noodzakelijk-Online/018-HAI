# Acceptance Test Matrix

Maps user-visible capabilities to the tests that prove them. "Automated" means a
Go test asserts it; "manual" means a documented step.

| Capability | Acceptance criterion | Coverage |
| --- | --- | --- |
| Remember context | Duplicate memories merge; retrieval ranks relevant ones | Automated — `memory/service_test.go` |
| Search memories | Authenticated owner/project/archive isolation; literal escaped search; exact tag, kind, sort and bounded pagination; database work respects request cancellation | Automated - `memory/query_test.go`, `memory/repository_query_postgres_test.go`, `memory/handler_test.go`, frontend context-memory service tests; live signed-browser search/filter/reset pass against the rebuilt stack |
| Large datasets | Pure Go compatibility fallback paginates correctly at 50k rows | Automated - `memory/largedataset_test.go`; production PostgreSQL scale/latency remains a release-target measurement gate |
| Hostile input | No panic; bounded output | Automated — `memory/adversarial_test.go` |
| Project isolation | No cross-project leakage | Automated — `memory/isolation_test.go` |
| Readiness | `/readyz` = 200 ready / 503 not-ready | Automated — `router/readiness_test.go` |
| Config self-check | `doctor` exits non-zero on failure | Automated — `doctor/doctor_test.go` |
| Security headers | Present on responses; Swagger CSP-exempt | Automated — `router/security_headers_test.go` |
| Rate limiting | 429 over limit; off by default | Automated — `router/rate_limit_test.go` |
| Ordinary mutation idempotency | Duplicate key -> 409 within the configured process-local replay window; opt-in | Automated - `router/idempotency_test.go` |
| Task operation identity | `/task/plan`, `/task/run`, and `/task/success` use an owner-scoped PostgreSQL operation ledger: one active claimant, exact completed replay, changed-input conflict, lease fencing, and fail-closed review after an expired/uncertain attempt | Automated - `task/task_operation_test.go`, `migrations/task_operation_identity_postgres_test.go`; live gateway replay documented in the operator verification record |
| Governed portfolio dispatch | A signed-in owner explicitly selects approved proposal items; each item is revalidated, authorized, and creates at most one review-gated local workflow. Exact retries replay or resume without creating approvals, running workflows, settling reservations, or invoking external providers. | Automated - `pursuit/portfolio_dispatch_test.go`, `migrations/pursuit_portfolio_dispatch_coordination_contract_test.go`, router permission coverage, and Pursuits service/component tests; live PostgreSQL append-only and invalid-provenance trigger probes |
| Controlled effect cancellation | An authenticated request cancellation or backend shutdown cancels in-flight allowlisted HTTP and Docker socket effects, and terminates complete subprocess trees for allowlisted scripts, Hermes CLI, OpenClaw CLI, and Docker readiness probes without waiting for their independent hard timeouts. Canceled runtime work is reported as blocked/stopped rather than completed or as an unexplained generic failure. | Automated - `automation/automation_service_test.go` exercises blocking HTTP and Docker socket servers plus a parent script with a long-running child; `processcontrol/process_test.go` proves shared process-tree termination; and `agentruntime/runtime_test.go` proves caller cancellation through both CLI adapters. Linux process-group termination passes, the Windows `taskkill /T /F` implementation cross-compiles, and every path retains a hard timeout as a second limit. |
| Connected-source cancellation | Closing an authenticated sync request or canceling a durable worker stops in-flight remote connector reads and optional semantic indexing. The source cursor is not advanced, and the failed job explicitly records the retained cursor for safe retry. | Automated - `source/service_test.go` cancels a blocking bounded JSON feed and `source/trello_test.go` cancels a credential-bearing Trello request while asserting prompt return with `context.Canceled`, no credential disclosure, a failed job, and unchanged source/job cursors. The same context is wired through GitHub, Gmail, Drive, Contacts, Calendar, Odoo JSON-2, ShareT, Airbyte, LARO, life-graph projection, and semantic indexing paths. Live-provider cancellation remains a provider acceptance gate. |
| Evidence-chain cancellation | Closing an authenticated source-search, task-plan, task-run, or verification request cancels blocked semantic retrieval, provenance resolution, claim projection, and downstream verification. Cancellation during evidence acquisition persists no claims or evidence and changes the durable verification run to `needs_review` with a generic stopped reason rather than reporting completion. | Automated - `source/search_context_test.go` blocks local semantic retrieval and proves prompt cancellation; `verification/context_test.go` proves evidence-acquisition cancellation leaves no accepted claims/evidence and records a reviewable stopped run; source, verification, and task handler tests prove the HTTP context reaches each context-aware service boundary. |
| Memory cancellation | Closing an authenticated semantic retrieval or explicit reindex request cancels local embedding/search work. Task planning passes the same request context into memory retrieval and does not continue with detached semantic work; trusted background callers retain the established context-free interface. | Automated - Memory service tests cancel blocked semantic search and reindex calls, while Task tests prove the planning request context reaches memory retrieval. Live endpoint disconnect remains an acceptance exercise. |
| Task preflight cancellation | Closing an authenticated task-plan or task-run request stops accepted-plan validation, advisory plan projection, whole-life context lookup, agent-registry reads, and due connected-source refresh before controlled execution. Durable schedulers and bounded post-effect audit settlement retain independent lifecycles. | Automated - focused Task regressions inject a canceled request context into all five pre-execution boundaries and prove the built-in agent/source implementations receive it; the canceled source preflight starts no provider work. Live disconnect during a retained provider call remains an acceptance exercise. |
| Catalog request cancellation | Closing a catalog revalidation/discovery request or stopping the backend cancels bounded GitHub and OSS Insight metadata calls, including scheduled maintenance, without persisting a network-cancellation result as catalog evidence. | Automated - `braincatalog/context_test.go` blocks each native external transport and proves caller cancellation reaches upstream metadata, collection review, and repository discovery. Existing maintenance and handler tests preserve bounded read-only behavior. |
| Frontend route payload | Login and unrelated routes do not load Control Center-specific CSS; operational modules remain lazy and component styles stay encapsulated. | Production Angular build with stats: compare initial raw/transferred totals and inspect route chunks; browser smoke Login and Control Center after the rebuilt frontend. |
| Frontend theme coverage | Every directly imported visual NG-ZORRO package has an explicit optimized theme entry, and dark-shell controls preserve readable active, disabled, hover, and focus states. | `npm run check:theme` is a production prebuild gate; deployed browser inspection verifies Memory pagination structure, computed dark-theme colors, and an empty console log. |
| Frontend shell change detection | The router root, shared progressive section, and authenticated shell use OnPush change detection. Navigation groups are precomputed; route changes still update the document title, module identity, and active navigation state. | All 405 Angular tests and the production build pass; deployed browser navigation from Command Center to Memory to Workflows verifies each title/identity/active-state transition with an empty console log. |
| LLM and task-effect cancellation | Closing an authenticated direct-generation or task-run request stops HAI waiting on the provider, records a redacted `stopped` model attempt, and does not add token or cost usage for the abandoned response. Task runs pass the same context to controlled automation launches and stop before verification or fallback work. | Automated - `llm/policy_test.go` cancels a blocking provider call and verifies prompt return, zero usage accounting, and a durable stopped record; `llm/handler_test.go` proves the direct HTTP context reaches that boundary. `task/service_test.go` proves a task-bound blocking model call stops without entering verification, while task handler and automation-executor tests prove context propagation to both final-effect paths. Whether a remote provider also stops its internal compute after disconnect remains a live provider acceptance gate. Durable workflow jobs retain their independent worker lifecycle. |
| Legacy-data owner isolation | Authenticated users see only exact-owner Pursuit, Source, Memory, conversation-archive, semantic-search, and Verification records. Only the explicitly configured `HAI_LEGACY_DATA_OWNER_IDENTITY` may additionally read ownerless pre-identity records; unset fails closed, other operators never receive them, and authenticated mutation remains exact-owner only. | Automated - identity, Pursuit, Source, Memory, memory-engine, and Verification tests prove the configured read-only migration exception, non-owner isolation, mutation denial, and the preserved unscoped internal-worker contract. Dry-run PostgreSQL query tests assert that the ownerless/null OR predicate is absent for normal operators and present only for the configured migration owner. |
| Advisory monitor collector accuracy | For each fixed source kind, seed a disposable owner-scoped PostgreSQL snapshot, run one target, and compare the numeric value and deterministic source digest with the expected workflow/open-loop, verified-completion, or overdue-commitment records. No arbitrary collector input is accepted. | **Automated against PostgreSQL** - a rollback-only temporary-table fixture mixes two owners plus future, archived, completed, unverified, terminal, and superseded records. All three fixed collectors return only the one eligible owner record and produce the same 64-character source digest on exact replay. Each count and bounded evidence snapshot now shares one read-only `REPEATABLE READ` transaction; a database-backed assertion verifies both transaction properties. External-account data is not part of these local-ledger collectors. |
| Monitor collection replay | Replay the same completed run/idempotency input before and after composition attempts. The existing immutable observation/run and its single composition delivery are reused without recollection or duplicate observation evidence; changed collection input conflicts. Collection replay must not report composition success while the delivery is pending or dead-lettered. | **Automated against PostgreSQL** - the disposable lifecycle runs migrations `0049`-`0051`, replays exact registration and completion, rejects changed-input reuse, retains one observation/run/delivery, and never recollects during composition retry. Retained signed-HTTP evidence remains required. |
| Monitor composition retry and receipts | Force a transient composition failure, verify an immutable failed attempt receipt and a future `pending` retry, then verify a later fenced claim can succeed without recollection. Exhaust the bounded policy separately and verify `dead_lettered`, immutable receipts, stale-worker refusal, and no duplicate downstream advisory record. | **Automated against PostgreSQL** - the mandatory integrated release lifecycle covers atomic enqueue, one transient failure, scheduled retry, success without recollection, pause/re-enable, active/expired collection lease recovery, exact replay, two-owner isolation, all three fixed collectors, and immutable attempt history. Focused service tests cover bounded dead-lettering and stale-worker refusal. A clean signed Windows gateway/UI due run retained one successful handoff and attempt; transient fault injection remains deliberately inside the isolated automated gate. |
| Monitor composition snapshot fidelity | Delay a delivery while changing the outcome definition, proactivity policy/feedback, or composer version, then demonstrate which snapshot the retry used. | **Automated locally and against PostgreSQL** - migration `0051` pins the exact outcome revision and audit digest, proactivity policy/signal/decision/feedback watermarks, composer version, capture time, and snapshot digest. Deterministic tests prove delayed replay continues to use the captured outcome and attention context after current state advances; the isolated PostgreSQL migration lifecycle proves legacy backfill and persisted snapshot constraints. The signed Windows UI displayed the pinned snapshot contract, composer version, outcome revision, context cutoff, watermarks, and immutable attempt receipt. |
| Monitor owner isolation | Create equivalent target IDs for two owners/workspaces. Target, observation, run, delivery, attempt-history, due-claim, and recovery reads reveal only the authenticated owner; foreign resource probes return no record. | **Automated against PostgreSQL** - repository and composition lifecycles exercise foreign owner/workspace reads, claims, deliveries, and attempts against a disposable database, while handler/router tests prove owner-derived scope and guarded permissions. A two-owner signed HTTP exercise remains required. |
| Monitor disable and lease fencing | A disabled target is not claimed for collection. An active unexpired collection or composition lease cannot be replaced or completed by another worker; only an expired lease can be recovered, after which a new generation may claim it. | **Automated locally and against PostgreSQL plus signed Windows UI** - the mandatory release lifecycle proves a paused target is skipped, active recovery returns zero, expired recovery returns one, re-enabled work is claimed under a new generation, and exact replay returns no work. Focused service/migration tests cover composition fencing. The 2026-08-14 signed owner flow paused and resumed the target, then recovered one valid expired crash lease through the owner-governed Advanced UI; final lease fields were null. |
| Monitor signed no-op and duplicate prevention | Load a retained owner-scoped monitor in Governance Control, invoke a bounded pass before its next due time, and prove truthful zero-work reporting without creating evidence or handoff duplicates. | **Accepted on the rebuilt Windows stack (2026-08-14)** - the signed UI reported `0 observations recorded; 0 failed. No work was executed or delivered`; read-only PostgreSQL counts remained 10 observations, 10 runs, and 10 composition deliveries. This is not a substitute for the remaining due-run and crash/recovery lifecycle gate. |
| Monitor no-effect boundary | Configure, collect, replay, compose, fail, retry, dead-letter, recover, pause, and resume a monitor while asserting zero task/runtime execution, notification/message delivery, Calendar write, workflow mutation, mandate authorization, external provider call, or controlled-learning mutation. Every target, evidence, delivery, and attempt authority flag stays false. | **Automated against PostgreSQL plus signed Windows UI for the due/pause/recovery path** - a whole-public-schema fingerprint permits changes only in the advisory monitor, outcome-evaluation, and proactivity ledgers; authority flags remain false and the inbox is asserted non-executable. Focused tests cover dead-lettering. The clean signed run reconciled zero workflow items/events, automation launches, execution-authorization receipts/consumptions, and final-effect exercises. |
| Governance monitor UI | In Governance Control, load an existing outcome indicator, create a fixed target, inspect status, run a bounded due pass, inspect immutable observations/runs, pause it, and resume it. Errors name a recovery path and duplicate clicks do not start duplicate work. | **Accepted on the rebuilt Windows stack (2026-08-14)** - the signed owner created an outcome and immutable target, ran one due pass, inspected its observation/run/pinned handoff receipt, paused and resumed it, and recovered one expired crash lease through the UI without console errors, page alerts, or authentication redirects. See `docs/evidence/ambient-monitor-windows-acceptance-2026-08-14.md`. |
| Provider fallback | Free before paid; never paid unless allowed | Automated — `providerfallback` |
| Autonomy gate | Risky/irreversible never auto-runs | Automated — `autonomygate` |
| Upload safety | Traversal/extension/size rejected | Automated — `upload`, `pathsafety` |
| Full-stack boot | A clean checkout generates fresh secrets, builds empty volumes, reaches healthy state, serves `/readyz`, signs in, and completes a bounded governed workflow | **Accepted on the current Windows host; repeat per release target** — `docs/fresh-clone-dryrun.md` |
| Accessibility / responsive | WCAG + breakpoints | **Pending** — frontend work |

## Gate

A change is acceptable when: `go vet` clean, `go test ./...` green, `/readyz`
ready against the target config, and any new user-visible capability has a row
here with automated coverage (or a documented manual step if automation isn't
yet feasible).
