# Completion Matrix

Honest status of the review items. Columns are deliberately separated so
"implemented" is never confused with "proven against a real account":

- **Implemented** — code, persistence, and API/contract exist in this repo.
- **Unit-tested** — focused automated coverage runs in `go test ./...` (no
  external services).
- **Sandbox-tested** — exercised against a disposable real dependency (e.g. a
  throwaway Postgres, a sandbox mailbox) — not production, but not a mock.
- **Live-tested** — a real credential/account completed a bounded, approved,
  end-to-end run with audit/verification evidence.
- **Deferred** — intentionally not done here, with the reason.

Legend: ✅ done · 🟡 partial · ⬜ not yet · — n/a

## 1. Live connectors

| Item | Implemented | Unit-tested | Sandbox-tested | Live-tested | Notes / Deferred |
| --- | :--: | :--: | :--: | :--: | --- |
| Trello **read-only REST** connector (`internal/source/trello.go`) | ✅ | ✅ | ✅ | ✅ | **Live-tested against a real Trello board (2026-07-23).** 7 cards ingested, 0 failed; every extraction carried its `trello.com/c/…` card provenance; cursor advanced to the newest `dateLastActivity`. **Incrementality proven live:** an immediate second sync ingested **0** cards. **Least privilege proven at the credential level:** the token reports `write=False` on both Board and Organization. The connector now requests descriptions, chronological comments, checklist state, and attachment metadata in the same bounded read, with attachment bodies deliberately left unfetched; this richer projection is covered by deterministic connector tests and still needs a fresh live complex-card acceptance run. Reproduce the existing live boundary with `go test -tags live -run Live ./internal/source/`. |
| — least privilege / no stored secrets | ✅ | ✅ | — | — | Creds from env only; no key/token/board id on the row. |
| — incremental sync + cursor | ✅ | ✅ | ✅ | ✅ | Cursor = max card `dateLastActivity`; unchanged cards skipped (test). |
| — pause / revoke | ✅ | ✅ | ✅ | — | Generic `Pause`/`Revoke` apply to the connector. Revocation atomically removes OAuth credentials and immediately excludes retained extractions from owner-scoped keyword retrieval, extraction listing, and pgvector retrieval, including stale semantic-cache matches. Records remain available only for governed administration and audit. |
| — provenance + audit logs | ✅ | ✅ | — | — | Card `shortUrl` on every extraction; audit via sync pipeline. |
| Trello **JSON export** (`project-board`, local) | ✅ | ✅ | — | — | Distinct `local_only` path; unchanged. |
| Gmail OAuth (consent, encrypted token, refresh, incremental, revoke, source links) | ✅ | ✅ | ✅ | ✅ | **Live-tested against a real sandbox mailbox (2026-07-23), all six assertions pass.** Consent via real authorization-code exchange; token stored as `bytea` ciphertext (281 bytes, no `ya29.` prefix); refresh verified by forcing expiry — ciphertext rotated and expiry advanced; 25 messages ingested with `mail.google.com` provenance on every extraction; revoke blocks sync (*"source is not enabled for sync"*); 25/25 source links retained after revoke. The run exposed **two real defects, both fixed in PR #25**: the grant carried `userinfo.email`/`profile`/`openid` beyond the declared `gmail.readonly` (caused by `include_granted_scopes`), and sync re-fetched all 25 messages each run instead of using a cursor — now **0 fetched** when no new mail. |
| Google Drive / Contacts / Calendar | ✅ separate read-only OAuth plus local export paths | ✅ | ✅ contract | — | Drive uses a bounded backfill plus native change token. Contacts uses a bounded People API backfill plus native sync token; imported people stay review-required candidates and removals become non-destructive tombstones. Calendar reads the primary calendar with a bounded one-year backfill plus native sync token; cancellations become non-destructive tombstones. Active opaque Calendar intervals now subtract from owner-confirmed task capacity, while transparent/cancelled events are excluded and source read failures fail closed. The resulting advisory schedule, blockers, and source links appear in Task Blueprint. No write-back exists. All three live connectors still need retained sandbox acceptance runs. |
| Documentation contradiction resolved | ✅ | — | — | — | `external-provider-reality-review.md` + README now report verified adapter status, not intent. |

## 2. Operational proof

The authenticated `/agent-teams` workspace now exposes the local governed
Agent Teams chain end to end: server-expanded advisory charter creation,
bounded membership and lifecycle, canonical evidence-backed votes, exact
message acknowledgments, deterministic consensus, attention state, and the
hash-linked audit ledger. The full backend suite, 379 frontend tests, direct
TypeScript compilation, cold Node 22.22.3 and Go production-image builds, and the
deployed authenticated route pass. Browser acceptance covered the persisted
team, post-consensus next-action state, console errors, and horizontal overflow
at desktop and the browser's narrow mobile floor. The frontend rejects any
response that claims execution authority and promotes consensus only after
distinct voters meet the threshold. Distributed A2A transport, cryptographic
peer identity, external delivery, and a live consensus cluster remain
explicitly unimplemented and are not implied by this UI.

The agent-team message acknowledgment path is implemented, unit-tested, and
sandbox-tested against real local PostgreSQL, but not live-tested against an
external A2A transport. Migration `0059` persists append-only owner-scoped
acknowledgments with exact message, team, version, recipient, correlation, and
digest provenance. Exact retries are idempotent, accepted or rejected state is
terminal, deferred state can progress, and attention distinguishes waiting,
deferred, acknowledged, rejected, overdue, expired, and not-required messages.
The full Go suite, 369 Angular tests, production build, and PostgreSQL lifecycle
test pass; the database test also proves direct mutation is rejected after
repository restart. This path cannot send reminders, approve work, authorize
execution, or establish live external multi-agent delivery.

| Item | Implemented | Unit-tested | Sandbox-tested | Live-tested | Notes / Deferred |
| --- | :--: | :--: | :--: | :--: | --- |
| Windows 11 fresh-clone acceptance run | — | — | 🟡 | ⬜ | The maintained Windows 11 Docker Desktop stack builds and runs locally, including a rebuilt frontend/backend, healthy dependencies, migrations through `0032`, signed local-preview authentication, and authenticated selector-v5 database round trips. A clean clone with empty volumes is still required before installation reproducibility is marked complete. |
| Browser E2E tests (`frontend/e2e/`) | ✅ | ✅ | ✅ | — | **Accepted against the rebuilt local Windows Compose stack (2026-08-04).** Playwright completed password login, owner-scoped read-only local source registration, bounded source sync, explicit pursuit creation, project-matched approval-gated workflow intake, exact runtime selection, durable human approval, exact selected-workflow execution of real `GET http://backend/readyz`, and terminal deterministic verification. The rebuilt worker also appended a real immutable `test_passed` completion attestation to the retained PostgreSQL volume; a live mutation attempt was rejected by the append-only trigger. Missing controls, failed proposal resolution, wrong execution route, non-terminal state, or absent verification evidence fail the suite. This proves the local read-only operator chain, not an external provider or mutable runtime side effect. |
| Health/readiness vs the **actual** stack | ✅ | ✅ | ✅ | ✅ | **Proven against a live stack**, not mocks. Backend booted against real Postgres 17 + Redis: `/readyz` reported `database.connection reachable in 12ms`, `redis.connection reachable in 1ms`, and correctly flagged an unreachable Kafka broker as `warn` (`degraded`/HTTP 200). **Killing Postgres flipped it to `not_ready`/HTTP 503** with `fail:1`, while `/healthz` stayed 200 (liveness ≠ readiness); restarting Postgres recovered it to `degraded`/200. |
| Owner-scoped outcome authoring, evaluation, and whole-life projection | ✅ | ✅ | ✅ | — | **Accepted against the rebuilt local Windows Compose stack (2026-08-03).** The signed-in Governance Control UI created immutable outcome revision 2, staged two user-confirmed dated observations, produced an advisory `on_track` evaluation, and reported its whole-life link. PostgreSQL retained one authenticated owner/workspace scope, non-empty ledger/graph digests, two outcome nodes, two workspace relations, and one evaluation-to-definition support relation. Projection retries are idempotent, repair definitions that predate graph wiring, expose redacted warnings without rolling back the outcome write, and reject authority-bearing projector responses. Browser-supplied owner identity is overwritten at the API boundary. This is local-stack acceptance, not evidence from an external provider or account. |
| Explicit outcome domains and governed execution context in the life graph | ✅ | ✅ | ✅ | — | New outcome definitions require one of the shared life domains; historical revisions stay readable with a review-marked fallback. Immutable execution-authorization receipts durably retain the bounded domain and project owner-scoped authorization outcome, verified approval evidence, commitment context, and non-incurred cost estimates. Source ingestion can project contact candidates only as low-confidence `needs_review` records. Full Go tests and PostgreSQL integration tests pass; no external side effect or trusted contact promotion is claimed. |
| Durable owner-scoped life commitment/cost ledger | ✅ | ✅ | ✅ | 🟡 | Migration `0026` defines append-only commitment revisions and distinct estimate/incurred/paid/refund cost events. Authenticated APIs use session owner scope, optimistic revisions, idempotency, payload/record digests, required provenance, terminal commitment states, and immutable database triggers. A fresh disposable PostgreSQL run proved revisions, replay/conflict handling, owner isolation, distinct cost semantics, verification-strength enforcement, and rejected update/delete/truncate. Governance Control now exposes Basic summaries, Advanced immutable history/evidence inspection, and authenticated authoring drawers for commitment revisions and cost evidence. The rebuilt local dashboard blocked a weak `paid` assertion and a refresh confirmed it was not stored. Real financial-account reconciliation and provider evidence remain open; the UI never initiates money movement. |
| Standing-mandate lifecycle projection into the whole-life graph | ✅ | ✅ | ⬜ | ⬜ | Persisted draft, active, and revoked mandate revisions project as separate restricted, local-only outcome nodes with source provenance and bounded project links. The projection is excluded from the mandate digest, cannot authorize or execute, and cannot roll back the authoritative mandate lifecycle; failures return redacted warnings. Focused tests cover lifecycle projection and rejection of authority-bearing graph responses. No distributed mandate execution or live runtime/provider acceptance is claimed. |

### Governed contact-review acceptance

| Item | Implemented | Unit-tested | Sandbox-tested | Live-tested | Notes / Deferred |
| --- | :--: | :--: | :--: | :--: | --- |
| Governed source-contact review | ✅ | ✅ | ✅ | — | Migration `0027` and authenticated life-ontology routes implement promote/correct/reject and merge/keep-distinct/reject decisions. Source candidates remain immutable; accepted identities append separate local-only, human-approved canonical people, and every decision explicitly grants no authority. Disposable PostgreSQL proved atomic canonical-plus-decision persistence, owner isolation, one winner for concurrent conflicting decisions, immutable update/truncate guards, safe empty rollback/reapply, and refusal to drop populated evidence. Governance Control exposes candidate/proposal review and immutable history. No external address book write or account reconciliation is claimed. |

### Durable automation approval acceptance

| Item | Implemented | Unit-tested | Sandbox-tested | Live-tested | Notes / Deferred |
| --- | :--: | :--: | :--: | :--: | --- |
| Durable single-use automation approval capability | Yes | Yes | Yes | No | Migration `0028` adds an owner-scoped append-only proof-consumption ledger; production requires a stable 32-byte deployment signing key and atomically claims one consumption before mutating API, script, Docker, or agent-runtime access. Disposable PostgreSQL proved cross-instance issuance/consumption, restart replay refusal, one winner among 24 concurrent consumers, immutable rows, safe empty rollback/reapply, and refusal to remove populated evidence. This prevents replay inside HAI; it does not claim exactly-once effects in external systems. |

### Workflow-to-runtime completion acceptance

| Item | Implemented | Unit-tested | Sandbox-tested | Live-tested | Notes / Deferred |
| --- | :--: | :--: | :--: | :--: | --- |
| Exact automation binding and completion verification | Yes | Yes | Yes | No | Execution-like workflow intake without an explicit automation now searches the owner-visible registry. One eligible match is bound automatically; zero or multiple matches create a review proposal and block generic approval until an exact runtime is selected. Read-only `GET`/`HEAD` tasks can complete without an LLM only when the persisted launch event, 2xx/3xx status, elapsed time, bounded output audit, and unified authorization receipt agree. Mutable or incomplete execution evidence remains review-gated. The local Windows Compose browser chain passed; no external provider or mutable effect is claimed. |
| Durable standing-mandate binding from intake to final effect | Yes | Yes | Yes | No | An explicit `mandateId` now survives Assistant command intake, pursuit candidate review/acceptance, workflow persistence, task preview/run, exact automation approval binding, and unified final-effect authorization. Migration `0032` adds owner-scoped pursuit/workflow bindings whose composite foreign keys reject unknown or cross-owner mandates. Immutable receipts bind the exact request, mandate snapshot, authorization decision, and approval proof digests; the service independently resolves and compares them again immediately before effect consumption, so revocation, expiry, revision drift, scope mismatch, stop conditions, or approval drift fail closed. Unit, security, workflow handoff, disposable-PostgreSQL, live local PostgreSQL migration, and frontend propagation tests pass. A mandate reference never grants authority by itself, ambiguous auto-selection is intentionally absent, and no broad external runtime/provider effect is claimed. |
| Pursuit outcome-contract and atomic resource enforcement | Yes | Yes | Yes | No | Migrations `0033` through `0040` persist the outcome contract, immutable owner-scoped effort/spend/refund and reservation/settlement ledgers, reconciliation reasons, durable task-operation identities, accepted portfolio allocations, execution-proposal snapshots, and per-item owner-decision history. Reservations and settlements commit their usage records, audit activities, and pursuit freshness in one transaction; audit failure rolls back the authoritative mutation, and idempotent replay adds no duplicate activity. Direct plan/run requests claim an owner/key/request/mode binding under a PostgreSQL advisory lock, heartbeat a fenced lease, replay the exact immutable completed plan, reject changed input, and move expired or lost leases to review rather than re-executing. Workflow retries use the workflow ID plus approval-decision provenance as their stable stage identity, so a pre-approval review cannot shadow later approved execution; pursuit reservations derive from the durable operation ID. PostgreSQL acceptance proves one concurrent operation winner, stale-worker rejection, immutable terminal state, resource ceiling serialization, and rollback refusal. Uncertain task operations now project idempotently into the existing owner-scoped review queue. The UI labels approval as a separate retry, requires deliberate confirmation, and can close the item without retry; the original operation remains immutable. Confirmed orphan release remains approval-protected and append-only. Proposal preparation, owner decision evidence, and portfolio execution are distinct. Concrete execution, external-provider acceptance, and exactly-once external effects remain open. |
| Owner-scoped advisory pursuit portfolio planner | Yes | Yes | Yes | No | Authenticated `POST /api/v1/pursuits/portfolio-plan` compares only the signed-in owner's open pursuits. The caller provides explicit optimistic/expected/pessimistic duration, usage, all non-capacity priority factors, planning horizon, and candidate availability windows; HAI does not invent missing estimates. Production wiring loads the latest owner-matched durable LifeOps capacity snapshot, recomputes 24-hour freshness using server time, requires confidence of at least 0.60 with no review flag, overrides browser capacity/energy inputs, merges overlapping windows, and caps them to confirmed available minutes. Human-approved estimate calibrations are shown separately as optional source-backed suggestions; an owner must explicitly copy one before planning, and the request binds its exact proposal, application, evidence digest, raw estimate, and reviewed estimate. A missing, changed, or rolled-back calibration fails closed while unselected learning never changes an estimate. Missing, stale, review-required, wrong-owner, and unavailable capacity returns a typed non-reservable result with a LifeOps recovery path. Priority evaluation remains pure and the deterministic scheduler respects pursuit dependencies, hard in-horizon targets, one-unit owner capacity, duration mode, request budget, and approval policy. The response is always `authority: advisory_only` and `canExecute: false`; it does not update pursuits, reserve capacity, consume approval, enqueue work, or write to a calendar. Live wearable capacity feeds and external-calendar acceptance remain open. |
| Governed pursuit portfolio allocation acceptance | Yes | Yes | Yes | No | Owner-only `POST /api/v1/pursuits/portfolio-plan/accept` requires the exact original planning request, deterministic decision digest, and `ACCEPT PORTFOLIO ALLOCATION` confirmation. The backend revalidates a fresh proposal, rejects digest drift, and atomically appends the immutable allocation, per-pursuit schedule rows, resource reservations, and pursuit audit activity under migration `0038`. Exact replay returns the existing record even though its own holds change later availability; changed reuse of a plan ID fails closed. Owner-scoped `GET /api/v1/pursuits/portfolio-allocations` returns bounded newest-first history only after revalidating stored parent/item evidence; the planner loads and reconciles it lazily after reload. High-risk items remain `accepted_needs_approval`. Every response is `authority: allocation_only` with `canExecute=false`: acceptance and history reserve or inspect bounded capacity but cannot approve, enqueue, execute, write a calendar, or claim an external effect. |
| Governed portfolio execution-proposal preparation | Yes | Yes | Yes | No | Owner-only `POST /api/v1/pursuits/portfolio-allocations/:allocationId/execution-proposals` requires the allocation's immutable record digest and exact `PREPARE EXECUTION PROPOSALS` confirmation. Migration `0039` appends owner-scoped proposal and item snapshots bound to accepted allocation-item and reservation IDs/digests plus current pursuit, policy, dependency, stop-condition, and settlement state. Status is derived as `prepared`, `prepared_needs_approval`, or `prepared_blocked`. An unchanged snapshot replays exactly; changed state creates a new immutable snapshot without rewriting prior evidence. Every result is `authority: proposal_only` with `canExecute=false`. This stage does not approve, consume approvals, enqueue, run tools or runtimes, settle reservations, or execute; live external execution is not claimed. |
| Durable portfolio execution-proposal recovery | Yes | Yes | Yes | No | Owner-only `GET /api/v1/pursuits/portfolio-execution-proposals?allocationIds=...` accepts 1-20 unique allocation IDs and returns at most the newest immutable proposal per requested allocation. The repository uses one bounded latest-per-allocation query, all proposal/item evidence is owner- and digest-validated, request order is preserved, and records remain `proposal_only` with `canExecute:false`. Backend-authored freshness metadata always requires revalidation and distinguishes a new snapshot from recovered historical evidence. Pursuits reloads proposals and decision histories after allocation history returns, then restores current coordination for one to 20 proposal IDs through one bounded owner-scoped batch read. It clears selections and confirmation phrases first, never preselects work, and fails closed on malformed, duplicate, foreign, authority-escalating, counter-invalid, false-current, duplicate-selection, or incomplete-selection responses. The full Go suite, route-permission coverage, 314 Angular tests, production build, isolated PostgreSQL query test, and deployed browser recovery pass. This is durable read recovery only; no approval, workflow creation, execution, settlement, or external effect occurs. |
| Per-item execution-proposal decision evidence | Yes | Yes | Yes | No | Owner-only `GET`/`POST /api/v1/pursuits/portfolio-execution-proposal-items/:itemId/decisions` accepts `approved`, `rejected`, `needs_clarification`, or `revoked` only with its exact decision-specific confirmation. Migration `0040` appends owner-scoped decisions and audit activity, links each successor to the previous decision, bounds approval to 30 minutes, and rejects update/delete. The service revalidates the immutable parent proposal, proposal item, captured pursuit state, latest decision, reservation settlement, and returned history chain. Full Go and Angular suites, disposable-PostgreSQL migration tests, and local authenticated browser approval/reload/revocation plus direct database verification pass. Every response remains `authority: approval_decision_only`, `canExecute:false`; this cannot enqueue, execute, mutate pursuit/reservation state, invoke a runtime/provider, or satisfy concrete final-effect authorization. Live external execution is intentionally not claimed. |
| Exact portfolio workflow-effect authorization receipt | Yes | Yes | Yes | No | Owner-only `POST /api/v1/pursuits/portfolio-execution-proposal-items/:itemId/authorize-workflow` accepts only the current item/decision digests and exact `AUTHORIZE PORTFOLIO WORKFLOW EFFECT` phrase. HAI derives one fixed reversible `workflow.intake` effect through `hai-workflow-engine` at EUR 0, reloads owner-scoped immutable evidence, refuses stale/revoked/expired/settled or changed state, and routes the approval source through the canonical no-fallback execution-authorization resolver. The UI shows the exact contract and resulting immutable receipt. Full Go, 285 Angular tests, and production build pass. Every response remains `authority: execution_authorization_only`, `canExecute:false`; the receipt is not consumed and no workflow, task, runtime, provider, reservation settlement, or external effect is created. |
| Single-use portfolio workflow creation | Yes | Yes | Yes | No | Owner-only `POST /api/v1/pursuits/portfolio-execution-proposal-items/:itemId/execute-workflow` requires an authorized receipt, exact item/decision digests, and `CREATE APPROVED PORTFOLIO WORKFLOW`. First use revalidates the current approval and atomically consumes the exact receipt authority for one consumer/target. Migrations `0042` and `0043` plus idempotent workflow intake ensure one active receipt-bound workflow and one keyed effect event. Exact retries load the linked workflow directly; interrupted retries can recover a missing link or audit event without repeated intake, receipt consumption, links, or effect activities. Historical audit rows are preserved. The workflow starts `needs_approval`, remains local and reversible, and is linked and audited. Full Go, 287 Angular tests, production build, and local API/browser replay acceptance pass. The response is `workflow_effect_executed`, `canExecute:false`; it does not run the workflow, invoke providers, settle reservations, or claim completion. |
| Governed portfolio dispatch coordination | Yes | Yes | Yes | No | Owner-only single and batch coordination previews operate on immutable execution proposals; the batch accepts 1-20 unique proposal IDs, preserves request order, uses fixed aggregate reads, caps combined items at 500, and fails the whole request when any proposal is unavailable to the owner. The route rejects a 21-ID request before storage access and has authenticated read-permission coverage. Robert must select 1-20 eligible items and enter `DISPATCH APPROVED PORTFOLIO WORKFLOWS`; nothing is preselected. Migration `0045` stores append-only run and item-attempt records, validates exact proposal/item/decision/receipt/workflow provenance in PostgreSQL, and refuses update/delete/truncate. Each selected item independently revalidates its current unexpired approval and policy before creating at most one receipt-bound `needs_approval` local workflow. Exact retries replay completed items and can resume partial runs; malformed owner, actor, digest, duplicate or incomplete selection, counters, or workflow evidence fails closed. The full Go suite, 314 Angular tests, production build, isolated PostgreSQL aggregate-query test, deployed browser recovery, retained-volume migration, and live append-only plus invalid-provenance trigger probes pass. This coordinator does not create approvals, run workflows, settle capacity, or call external providers, so real-world execution acceptance is intentionally not claimed. |
| Exact reviewed workflow run command | Yes | Yes | Yes | No | Owner-only `POST /api/v1/workflow/:id/run` atomically claims only the named ready and approved workflow, reuses the existing task/runtime authorization and verification worker, and returns the real terminal, retry, blocked, review, or skipped outcome. Duplicate invocation cannot run a completed item, foreign owners receive no access, emergency stop remains effective, and the Workflow Engine requires deliberate confirmation. Focused service/handler/router tests, all 289 Angular tests, and the local login/source/sync/workflow/approval/exact-run Playwright chain pass. External mutable-provider acceptance is not claimed. |
| Owner-scoped internal reminder proposals | Yes | Yes | Yes | No | `GET /api/v1/workflow/reminder-proposals` projects open checklist `ReminderAt` records for only the authenticated owner across a bounded 1-720 hour horizon and 1-200 item limit. Completed, archived, closed-checklist, ownerless, and foreign-owner records are excluded. Every item and snapshot is `reminder_proposal_only`, `canExecute:false`, current but revalidation-required, and counter/identity validated by the Workflow Engine. Focused service/handler/router tests, a real PostgreSQL owner-isolation query test, 324 Angular tests, and the production build pass. This read model does not persist approval, activate a reminder, schedule a notification, send a message, run a follow-up, or write Calendar data. |
| Append-only reminder activation request/decision ledger | Yes | Yes | Yes | Local stack | Migrations `0046` and `0047` plus owner-scoped workflow APIs append immutable `internal_notification` preparation requests and chained `approved`, `rejected`, `needs_clarification`, or `revoked` decisions. Requests bind the current workflow/checklist reminder digest, actor/owner, idempotency key, exact confirmation, and expiry; decisions bind the immutable request digest, reason, previous chain tip, exact decision confirmation, and any approval expiry. `0047` rejects a decision timestamp that does not strictly advance beyond the current chain tip. Database constraints and triggers reject foreign/stale sources and update/delete/truncate. Preparation and approval still cannot deliver a reminder or grant effect authority; delivery requires the separate exact authorization below. |
| Internal reminder delivery worker and receipt ledger | Yes | Yes | Yes | Local stack | Owner-only authorization binds the exact current preparation, approval decision, reminder digest, `in_app` channel, idempotency key, and `AUTHORIZE ONE INTERNAL HAI REMINDER`. Migrations `0055` through `0057` preserve append-only authorization/attempt evidence, database-level source and decision provenance, exactly one authorization per approved decision/channel, at most three attempts, terminal suppression, and explicit `dead_lettered` receipts. The workflow sweep revalidates revocation and source drift before every attempt and hands successful delivery only to the local owner-scoped proactivity inbox. Replay uses stable evidence rather than generated IDs/timestamps. Workflow Engine exposes the explicit single authorization, bounded owner-scoped due pass, compact status, and Advanced receipt provenance. Focused service/route/migration tests, live PostgreSQL replay, all 367 Angular tests, and production build pass. This path cannot send email/messages, write Calendar data, invoke webhooks/desktop push/providers, run workflows, or perform external follow-up effects. No external notification acceptance is claimed. |
| Immutable proactive-attention feedback | Yes | Yes | Yes | No | Migration `0048`, owner-scoped GET/POST APIs, the evaluator, and Governance Control support accept, exact-revision dismiss, bounded snooze, indefinite suppression, and resume. Records are append-only, chained, idempotent, source-decision-bound, secret-rejecting, and always `attention_feedback_only` with delivery/execution authority false. Unit, router, Angular, migration-contract, and isolated PostgreSQL lifecycle tests cover replay after a successor, owner isolation, source binding, and update/delete/truncate rejection. This controls dashboard attention only; it does not send notifications or execute work. |
| Verified portfolio reservation settlement | Yes | Yes | Yes | No | Owner-only `POST /api/v1/pursuits/portfolio-execution-proposal-items/:itemId/settle-workflow` requires the exact item digest, linked workflow ID, measured effort/cost, and `SETTLE VERIFIED PORTFOLIO WORK`. Migration `0044` makes the normal worker append an immutable completion attestation only for `verified` or `test_passed` outcomes. Settlement locks and revalidates the proposal item, original approved decision, reservation, authorization receipt and exact consumption, single receipt-bound pursuit link, completed workflow projection, and attestation before atomically appending the ledger settlement, portfolio proof, effort/cost events, and activity. It then records one idempotent controlled-learning outcome from the proof, attestation, receipt, and estimated-versus-actual usage. Three comparable owner/project-scoped outcomes may create a deterministic median/MAD calibration proposal with bounded factors, confidence, observation window, and exact evidence digest. Approved, rejected, and rolled-back reviews anchor the evidence window, so a subsequent proposal requires three fresh comparable outcomes. An unresolved proposal remains the single scope review item; later evidence is counted and checked for material drift without creating competing approvals. The proposal remains inert until explicit owner approval through Governance Control; rollback removes it from planning eligibility. A learning outage cannot invalidate accounting, and replay retries evidence without repeating settlement or execution. Exact replay remains valid after workflow archival; changed usage, weak verification labels, forged mutable completion, missing attestation, or duplicate receipt links fail closed. This is explicit verified accounting and governed learning, not automatic self-modification, external-provider acceptance, or new execution authority. |
| Server-owned system workload classification | Yes | Yes | Yes | No | The local safe worker, task-runtime launcher, and local model-maintenance worker have exact operation, stage, resource, tool/runtime, authority, autonomy, risk, reversibility, and zero-cost contracts. Unknown system actors or caller-declared downgrades are denied before later policy layers. The policy is rechecked before receipt consumption and exposed through redacted inspection evidence. This does not validate an external runtime or provider. |
| Selector-v5 execution contract binding | Yes | Yes | No | No | Task-plan digests, workflow provenance, side-effect-free preview comparison, and unified authorization receipts retain the selected framework risk ceiling, maximum autonomy level, and exact-approval requirement. Over-ceiling execution and missing framework-required case approval fail closed. This proves internal enforcement, not an external provider effect. |
| Exact owner-scoped immutable selector-v5 resolution | Yes | Yes | No | No | Execution authorization resolves the selection by authenticated owner and exact UUID, independently compares IDs, versions, risk, autonomy, approval, and all governance digests, and denies with `framework.selection_unverified` before ordinary policy evaluation on absence or mismatch. `AuthorizeAndConsume` repeats the comparison before the single consumption reservation. This proves repository and authorization behavior, not a configured external runtime. |
| Main router and Phase2 resolver composition | Yes | Yes | No | No | Both `router/routes.go` and `phase2/module.go` install the immutable Framework Registry adapter and durable framework-evidence preflight adapter on the execution-authorization service; the task engine shares that preflight repository. A selector-v5 path therefore fails closed if either exact resolver cannot be constructed. This has focused package coverage; no external provider or account is exercised. |
| Typed three-phase framework evidence contract | Yes | Yes | No | No | Planning preserves each source framework and compiles a deterministic requirement ID, required flag, validator, freshness limit, and `pre_authorization`, `execution`, or `postcondition` phase. Applicable pre-authorization requirements are evaluated before any allowed step; missing evidence blocks with `needs_review` and records zero external effects. Typed execution/postcondition requirements use exact phase-specific evaluators and fail closed when unsupported; the fuzzy matcher remains only for historical string-only plans. Provider-specific live assertion producers remain untested. |
| Framework preflight provenance binding and independent resolution | Yes | Yes | Yes | No | A canonical digest binds owner, task plan, framework selection, evaluation time, assertions, evidence, and failures. Before controlled tool/local execution, the passing tuple is stored in the append-only owner-scoped 0030 ledger. Selector-v5 authorization resolves it exactly and repeats the check before receipt consumption; persistence failure records zero executor calls. Live PostgreSQL migration, immutability, constraint, replay, and owner-isolation checks pass. No external provider effect is claimed. |
| Exact source evidence and semantic-status separation | Yes | Yes | Yes | No | Connected-source evidence is resolved to the exact owner-scoped extraction, raw item, active source, URI/hash/project tuple, and raw-item timestamp before authorization and again before receipt consumption. Grounded-answer retrieval independently matches the returned extraction payload to that durable snapshot, deduplicates repeated hits, and records matching text as `source_supported`, never `verified` solely from lexical similarity. Missing, foreign, stale where required, or mutated evidence fails closed. This proves provenance and bounded support, not semantic truth or external source authority. |
| Completion-first model outcome calibration | Yes | Yes | Yes | No | Every real LLM generation attempt that reaches a provider/model now creates a redacted `model_run_telemetries` record containing operational metadata rather than prompt or output content. Task validation updates the exact generation telemetry row with its trusted outcome and method; Model Intelligence refreshes calibration from that durable ledger, including records written outside its own process-local path. Route selection prefers accepted-output evidence conservatively, using the lane-specific lower confidence bound and evaluated sample count before efficiency tie-breakers, while avoiding a dead end when all candidates remain weak or unevaluated. Direct generation API calls remain `unvalidated` until a trusted validator records an outcome. Migration `0031`, focused routing/calibration/task tests, and the authenticated local Model Intelligence surface cover the local contract; external-provider acceptance still requires real credentials, live calls, and retained provider-specific evidence. |
| Immutable plan graph and exact orchestration binding | Yes | Yes | Yes | Local stack | Migration `0052` stores append-only owner-scoped plan revisions with database mutation guards. Migrations `0053` and `0054` bind workflows and accepted pursuit portfolio allocations to the exact owner, plan ID, revision, digest, and selected node through all-or-none shape checks and immutable foreign keys. Task planning/runs, workflow intake/worker execution, pursuit intake/planning, portfolio proposal approval, dispatch, effect authorization, and first receipt consumption resolve and revalidate the same latest accepted revision. A durable task `Plan` with no accepted reference now projects an immutable revision-1 draft graph bound to the exact task operation and authenticated owner. Task `Preview` and task `Run` do not create this state, projection failure fails closed, and accepted provenance suppresses a competing draft. Plan acceptance remains coordination-only and `canExecute` is always false; approval and execution authority stay separate. Terminal history and already-consumed crash recovery use a distinct historical-provenance resolver and cannot authorize new work. The full Go suite, repeated proposal-history regression, Compose validation, live backend rebuild, migrations through `0054`, PostgreSQL schema inspection, and `/readyz` pass. Automatic graph projection from pursuit planning, connected-source records, and other intake paths plus external-provider acceptance remain separate work; no local LLM provider is configured in the verified stack. |
| Automatic workflow Plan Graph draft projection | Yes | Yes | Yes | Local stack | Migration `0058` adds a separate all-or-none workflow draft reference and rejects a selected node that does not bind the same authenticated owner and workflow. Governed pursuit-candidate acceptance and ordinary workflow intake project one immutable revision-1 advisory graph from the persisted checklist when no accepted plan or previous draft exists. The workflow stores the exact plan ID, revision, digest, and root node; projection failure blocks otherwise-ready work and unchanged-source replay retries idempotently. Accepted-plan provenance suppresses a competing draft. Focused projection/migration tests, the full Go suite, 368 Angular tests, production build, live backend/frontend rebuild, authenticated browser candidate acceptance and deep-link inspection, and a live PostgreSQL forged-node rejection pass. The draft is planning evidence only and cannot approve or execute work. Pursuit planning, source-record projection, and external-provider acceptance remain separate work. |

### Advisory ambient outcome monitor

| Item | Implemented | Unit-tested | Sandbox-tested | Live-tested | Notes / Deferred |
| --- | :--: | :--: | :--: | :--: | --- |
| Fixed read-only collectors | Yes | Required acceptance | Required acceptance | No | The implementation exposes only workflow open-loop count, verified workflow completion count, and overdue commitment count from canonical local ledgers. Targets cannot provide SQL, URLs, scripts, expressions, or tool instructions. Parent verification must compare values and source digests against a disposable PostgreSQL snapshot before this column is upgraded. |
| Immutable observation and run ledgers | Yes | Required acceptance | Required acceptance | No | Migration `0049` adds append-only owner/workspace/outcome/indicator evidence and lease-fenced run receipts, plus mutable target scheduling state that must advance revision monotonically. Exact completed replay must not recollect or duplicate evidence. A completed run proves collection persistence only; it does not prove outcome/proactivity composition succeeded. Rollback refuses non-empty tables. |
| Durable advisory composition handoff | Yes | Yes | Yes | No | Migration `0050` creates one owner-scoped delivery for each successful run, idempotently backfills earlier successful runs, and records `pending`, `succeeded`, or `dead_lettered` state separately from collection. Revision/lease-generation fencing protects claims; bounded retries append immutable success/failure attempt receipts with redacted failure codes. Focused migration contract and disposable-PostgreSQL lifecycle tests cover backfill, retry, success, dead-lettering, stale-worker rejection, immutability, owner isolation, and guarded rollback. Full scheduler/API/UI acceptance and crash-boundary verification remain open. |
| Durable advisory sweep | Yes | Yes | Local stack | No | A singleton `outcome-monitor.sweep` job discovers bounded due collection and composition scopes, recovers expired leases, and processes bounded batches. It obeys the shared background safety predicate. Active leases must remain fenced; disabled targets must not be claimed. All in-process schedulers now share the signal-aware backend application context, and Compose allows a 20-second drain window. High availability and distributed scheduling remain absent. |
| Outcome/proactivity composition | Yes | Yes | Required acceptance | No | A successful handoff attempt can map immutable monitor evidence into source-supported outcome evidence and an idempotent owner-attention decision/inbox item. A failed attempt remains pending for bounded retry or becomes dead-lettered; neither state changes the already-completed observation/run. Migration `0051` pins the exact outcome revision and audit digest, proactivity policy/signal/decision/feedback watermarks, composer version, capture time, and snapshot digest before the completed monitor run is committed. Every attempt binds that snapshot digest; legacy unpinned deliveries are dead-lettered instead of being recomposed against current state. Deterministic package tests prove replay still uses the pinned outcome and attention context after current state advances, and the time-dependent test harness now uses one injected clock. Disposable-PostgreSQL lifecycle and live scheduler/API/UI acceptance remain open. This is advisory composition only, not workflow execution or notification delivery. |
| Governance Control and guarded routes | Yes | Required acceptance | Required acceptance | No | Governance Control can configure a fixed collector, select a target, pause/resume it, run a bounded due pass, and inspect observations/runs. API routes require authenticated owner scope, a recognized role, and read/write/admin permissions as appropriate. Signed-in browser acceptance remains required. |
| No-authority boundary | Yes | Required acceptance | Required acceptance | No | Targets, observations, runs, composition deliveries, and attempt receipts remain `advisory_monitor_only`; all execution, delivery, notification, external-effect, and learning-mutation capability flags are false. Required acceptance must prove no task/runtime execution, notification, message delivery, Calendar write, workflow mutation, mandate authorization, external provider call, or learning mutation occurs during configuration, scheduled/manual runs, composition retries, dead-lettering, or recovery. |

### Governed ngrok HTTPS profile

The disabled-by-default `cloud-tunnel` profile is implemented and
contract-tested. It uses a digest-pinned ngrok agent and exposes only nginx on
the private Docker network. Container and Windows preflight gates reject local
login bypass, insecure cookies, non-production mode, non-loopback gateway
binding, missing tokens, and invalid public origins; the Windows gate also
rejects placeholder HAI secrets and mismatched Google callbacks. Compose and
ngrok v3 configuration validation, shell syntax, positive and negative gate
cases, and Windows CI contracts pass. A real ACL-restricted token/domain and a
retained external login acceptance run remain required before this path is
live-proven.

The bounded A2A planning bridge is now reachable through nginx both locally and,
only with a second explicit production opt-in, through the same fixed ngrok
origin. Browser auth is intentionally not accepted on those two exact routes;
`SendMessage` requires its separate bridge token and nginx applies a dedicated
request throttle and 16 KiB body limit. Backend, gateway, and Windows smoke
tests prove the local Agent Card, anonymous denial, authenticated planning
response, and no-execution scope. The Windows and container tunnel gates reject
misaligned public bridge configuration. A real public tunnel run remains an
external acceptance requirement and no full distributed A2A lifecycle is
claimed.

## 3. Production engineering

| Item | Implemented | Unit-tested | Sandbox-tested | Live-tested | Notes / Deferred |
| --- | :--: | :--: | :--: | :--: | --- |
| Versioned DB migrations (`backend/migrations/`, `internal/infra/migrate.go`) | ✅ | ✅ | ✅ | ✅ | Runner + `schema_migrations` + two-phase (pre/post) + `migrate status\|up\|down` CLI. **Verified against real Postgres 17**: full schema apply, idempotency, rollback+re-apply. Destructive lifecycle tests now create one temporary database per test and load only the ordered migration chain through the target, so they cannot modify the configured HAI database or collide with later migrations. |
| Replace production reliance on AutoMigrate | ✅ | ✅ | ✅ | ✅ | **Done.** Generated baseline (`pre/0002_baseline`, 53 tables / 301 indexes / 56 guarded constraints) and `DB_AUTOMIGRATE` now defaults to **false**. Proven 3 ways on real Postgres 17: fresh DB builds all 54 tables from migrations alone; re-run idempotent; baseline safe over an existing AutoMigrate-built DB. Regenerate via `scripts/generate-migration-baseline.sh`. |
| Durable worker (scheduling/retry) | ✅ | ✅ | ✅ | ⬜ | **Built** (`internal/durablejob`): persisted jobs, `RunAt` scheduling, bounded retry with backoff, dead-lettering, lease-based crash recovery, panic containment. 8 tests pass incl. **real Postgres**: survives process restart, 25 jobs × 2 concurrent workers each executed exactly once (`FOR UPDATE SKIP LOCKED`), orphaned leases reclaimed. **All three schedulers now run on it** — source (`source.scan` → one retryable `source.sync` per due source), workflow (`workflow.sweep`), and ambient (`ambient.scan`), via a shared `RegisterRecurring` helper that keeps each a self-rescheduling singleton. Each falls back to its legacy in-process ticker (with a log line) if the queue is unreachable. Recurring jobs reschedule on success **or** final attempt, so a burst of failures cannot silently kill a schedule. |
| Two-account isolation | ✅ | ✅ | ✅ | ✅ | **Live-proven against a running stack (2026-07-23), 9/9 assertions pass.** Two authenticated owners over real HTTP + real Postgres: source listing, extracted content, grounded search, and sync history are all owner-scoped, and the second owner cannot pause/sync/revoke the first owner's source by id — each returns **404, not 403**, so the refusal does not confirm the resource exists. Repeatable: `scripts/two-account-isolation-test.sh` (exits non-zero on first failure). Complements the unit-level test. |
| Risky runtimes disabled until gated | ✅ | ✅ | — | ⬜ | Agent runtimes, browser automation, paid providers, and external side effects are disabled by default behind approval boundaries. Per-runtime **live** integration tests remain deferred gates. |

## 4. Delivery hygiene

| Item | Status | Evidence |
| --- | :--: | --- |
| `.gitignore` excludes env backups (`.env.local.bak`, `*.bak`) | ✅ | `git check-ignore` confirms `.env.local.bak` ignored. |
| No local backups / personal fixtures / diagnostics in the archive | ✅ | `connected-sources/*.eml`, `connected-sources/summary`, `HAI-Work-Report.pdf`, `.claude/` now ignored; only `.gitkeep` tracked in data dirs. |
| No real credentials tracked | ✅ | Tracked `.env*` files are Docker-compose **dev defaults** (`postgres/postgres`, `JWT_SECRET=secret`), not production secrets. |
| Completion matrix | ✅ | This document. |

## Remaining external gates (need resources outside this environment)

> ✅ **Trello live run closed 2026-07-23** — see §1.
> ✅ **Two-account isolation closed 2026-07-23** — see §3.
> ✅ **Gmail sandbox acceptance closed 2026-07-23** — see §1 (two defects found and fixed).
> ✅ **Owner-scoped outcome local-stack acceptance closed 2026-08-03** — see §2.
> ✅ **Local browser operator chain closed 2026-08-04** — see §2.

1. **Windows 11 fresh-clone run** — `docker compose up --build` + the full operator flow on a clean Windows host.
2. **Per-runtime live tests** — one controlled integration test + approval boundary each for agent runtimes, browser automation, and any paid provider before enabling.
3. **External commitment/cost reconciliation** — verify sourced obligations and financial events against explicitly authorized provider accounts before treating the local ledger as reconciled operational truth.
4. **Standing-mandate runtime acceptance** — prove that a configured runtime honors exact scope, expiry, approval, risk, stop-condition, and revocation decisions end to end; the advisory life-graph node is not evidence of enforcement by an external runtime.

5. **External contact reconciliation** — verify approved canonical contacts against an explicitly authorized address-book provider; local review decisions do not write to external accounts.
6. **Least-privilege runtime database identity** — separate migration ownership from the backend runtime role and enforce minimum table privileges plus owner isolation before production deployment.

> Closed since the first pass: the migration baseline (AutoMigrate is now off by
> default) and the durable worker model are both built and verified against real
> Postgres — they are no longer external gates.

## Reproduce the automated evidence

If the matching local toolchain is unavailable, use the pinned backend
container (Go 1.25.12):

```bash
# Unit tests (no external services)
docker run --rm -v "$PWD/backend":/app -w /app golang:1.25.12 go test ./...

# Trello connector tests
docker run --rm -v "$PWD/backend":/app -w /app golang:1.25.12 \
  go test ./internal/source/ -run Trello -v

# Migration runner vs REAL Postgres 17 (data dir on tmpfs so it needs no disk)
docker network create hai-net
docker run -d --name hai-pg --network hai-net --tmpfs /var/lib/postgresql/data \
  -e POSTGRES_PASSWORD=postgres -e POSTGRES_DB=automation_hub postgres:17-alpine
docker run --rm --network hai-net \
  -e HAI_TEST_DATABASE_DSN="host=hai-pg user=postgres password=postgres dbname=automation_hub port=5432 sslmode=disable TimeZone=UTC" \
  -v "$PWD/backend":/app -w /app golang:1.25.12 \
  go test -tags integration -run 'Migrat|Rollback' ./internal/infra/ -v
```
