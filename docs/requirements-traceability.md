# Requirements Traceability

Traces the goal prompt's critical-path requirement to the code and tests that
satisfy each link, so every requirement has a verifiable home.

## Critical path

`dashboard → source ingestion/memory → task planning → LLM/tool routing →
approval gates → controlled execution → verification → workflow state → audit`

| Requirement link | Implementation | Test/evidence |
| --- | --- | --- |
| Authenticated dashboard | `idp/`, API-key middleware, `fe/pages/login` | router smoke; `session` |
| Source ingestion | `be/source`, `connected-sources` | `source/service_test.go` |
| Memory | `be/memory` | `memory/service_test.go`, `query_test.go` |
| Task planning | `be/task` | `task/service_test.go` |
| Immutable plan coordination | `be/plangraph`, migration `0052_plan_graph_contract`, authenticated `/api/v1/plans` routes, and `fe/pages/plan-coordination` | service, handler, repository, migration-chain, RBAC, Angular service/component/registry tests; live PostgreSQL create, stale-digest rejection, accept, replan, historical revision, list, restart-persistence, desktop inspector, and narrow-viewport acceptance |
| Governed advisory agent teams | `be/frameworkregistry/agent_team_*`, canonical `be/agentcoordination` envelopes, authenticated `/api/v1/framework-registry/teams` routes, migration `0059`, and `fe/pages/agent-teams` | guided server-owned charter, decision-message, and acknowledgment tests; non-voting and duplicate-voter rejection; authenticated route guards; frontend authority-response rejection, distinct-voter readiness, progressive-disclosure, and guided endpoint tests; full backend and 379-test frontend suites; cold Node 22.22.3 and Go production builds; deployed authenticated route, console, next-action, and responsive-overflow checks. The UI supports team lifecycle, exact member roles, evidence-backed votes, acknowledgments, deterministic consensus, and hash-linked audit inspection, but never grants execution authority. |
| Verified runtime role resolution | `be/frameworkregistry/operating_context.go` maps only allowlisted control-plane roles to named deterministic HAI module dependencies; domain or external specialist roles require fresh verified agent cards. `be/task/service.go` blocks every non-ready delegation regardless of `single_engine` coordination. | focused framework/task tests, complete Go suite, and authenticated live acceptance: the bounded readiness automation used one verified embedded coordinator and completed; a medication request required `health_admin_assistant`, retained a `requires_assignment` delegation, and produced no tool execution. |
| Resource-aware bounded automation | `be/task/resource_planning.go` distinguishes low-risk configured automation from new owner commitments; `be/task/service.go` classifies explicit read-only automations, selects `automation.launch`, and skips connected-source retrieval when the task has no source-context dependency. | unit regressions cover unknown owner capacity, deterministic no-model execution, source-search suppression, destructive refusal, and specialist blocking. Live local acceptance verified feasible resource planning, 24/24 preflight assertions, a completed API launch, `test_passed`, and final `validated` state without model or paid usage. Resource planning remains advisory and grants no authority. |
| LLM/tool routing | `be/llm`, `be/router`, `providerfallback` | `llm/*_test.go`, `providerfallback` |
| Approval gates | `be/safety`, `be/workflow`, `autonomygate` | `safety/*`, `autonomygate` |
| Controlled execution | `be/automation`, `be/agentruntime` | `automation/*_test.go` |
| Restart-safe single-use automation approval | `be/automation/approval_proof.go`, `approval_proof_postgres.go`, migration `0028_automation_approval_proof_consumptions` | unit binding/replay tests; disposable-PostgreSQL restart, 24-way concurrency, immutability, empty rollback/reapply, and populated rollback-refusal acceptance |
| Framework-bound execution ceiling | `be/task/governance_binding.go`, `be/workflowtask/runner.go`, `be/executionauth` | selector-v5 autonomy-overreach, case-approval, preview-drift, legacy-v4, full backend, and Workflow Engine frontend tests |
| Exact owner-scoped selector-v5 resolution | `be/frameworkregistry.Service.Selection`, PostgreSQL and memory `GetSelection`, `be/executionauth/framework_selection.go`, and `framework_selection_adapter.go` | `frameworkregistry/repository_test.go`, `repository_postgres_test.go`, and `executionauth/framework_selection_test.go` cover exact lookup, cross-owner refusal, resolver absence, forged contract mismatch, immutable match, and pre-consumption recheck |
| Resolver composition in every execution-authorization path | `be/router/routes.go` and `be/phase2/module.go` install both `executionauth.NewFrameworkSelectionResolver` and `NewFrameworkEvidencePreflightResolver`; the task service shares the same durable preflight repository | focused `router` and `phase2` package tests plus compile-time interface wiring; this is composition proof, not a live external-runtime acceptance run |
| Phase-aware framework evidence contract | `be/task/framework_evidence.go`, `ValidationPlan.frameworkEvidenceContracts`, and `CompletionPlan.frameworkEvidencePreflight` | `task/framework_evidence_test.go` covers deterministic ownership/phase compilation, zero-effect preflight blocks, exact successful preflight, exact launch receipts, postcondition isolation, and historical-plan compatibility; unsupported typed evidence fails closed |
| Independently resolved preflight provenance | canonical `framework-evidence-preflight-v1` digest is included in task governance, task-plan digest, authorization request/receipt, and evidence references; controlled tool/local execution persists migration `0030_framework_evidence_preflights` before invoking the executor | task zero-effect persistence-failure test; memory/GORM owner isolation, immutable replay, canonical digest, adapter, resolver absence, forged/failed/foreign record, inspection, and pre-consumption drift tests; live PostgreSQL 0030 constraint/immutability and repository round trip |
| Exact source-evidence preauthorization | `be/sourceevidence` resolves the exact owner-scoped extraction, raw item, and active connected source; `be/task/framework_evidence.go` binds source claims into the preflight; `be/executionauth/framework_evidence_preflight.go` re-resolves them before authorization and before receipt consumption | `sourceevidence/repository_test.go` covers exact joins, owner isolation, source/extraction state, URI/hash/project integrity, raw-item freshness, and source-wide sync non-refresh; `executionauth/framework_evidence_preflight_test.go` covers exact success, missing/foreign/stale/mutated claims, and pre-consumption drift without receipt consumption |
| Verification | `be/verification` | `verification/handler_test.go` |
| Temporal claim assessment and review | `be/knowledgegraph/claim_assessment.go`, authenticated `/api/v1/knowledge/claims` routes, `router/knowledge_claim_projection.go`, and `fe/pages/knowledge-claims` | owner/workspace, automatic grounded-verification projection, corroboration, conflict, effective/observed time, transitive supersession, bounded review queue, progressive disclosure, truncation, corrupt-storage, handler, frontend, and RBAC tests in `verification`, `knowledgegraph`, `router`, and Angular |
| Outcome definition, longitudinal evaluation, and whole-life projection | `be/outcomeevaluation`, `be/lifeontology`, `fe/pages/governance-control` | authenticated handler/projection tests; frontend component/service tests; local Compose browser-to-Postgres acceptance |
| Durable commitments and costs | `be/lifeledger`, migration `0026_life_commitment_cost_ledgers`, `fe/pages/governance-control` | full Go suite against disposable PostgreSQL; component/service tests; rebuilt authenticated local UI authoring and weak-payment refusal acceptance |
| Governed contact review | `be/lifeontology/contact_review.go`, migration `0027_contact_review_decisions`, `fe/pages/governance-control` | focused service/handler/RBAC/component tests; disposable-PostgreSQL atomicity, concurrency, immutability, rollback, and owner-isolation acceptance |
| Standing-mandate lifecycle context | `be/standingmandate`, `be/lifeontology` | `standingmandate/life_ontology_projection_test.go`; distributed runtime acceptance remains open |
| Explicit standing-mandate execution binding | `be/assistant`, `be/pursuit`, `be/workflow`, `be/workflowtask`, `be/task`, `be/automation`, `be/executionauth`, migration `0032_pursuit_workflow_standing_mandate_binding`, and `fe/pages/task-blueprint` | focused propagation, malformed/conflicting reference, owner isolation, approval-proof binding, immutable snapshot/decision comparison, pre-effect drift, disposable-PostgreSQL, live local migration, and Angular request tests; external mutable runtime acceptance remains open |
| Bounded pursuit outcome contract and atomic resource enforcement | `be/models/pursuit.go`, `be/pursuit`, `be/task/resource_planning.go`, migrations `0033_pursuit_goal_contract` through `0040_pursuit_portfolio_execution_proposal_decisions`, and `fe/pages/pursuits` plus `fe/pages/task-blueprint` | validation, false-completion, evidence, triggered-stop, overdue-target, blocked-dependency, parallel-work ceiling, cadence, owner-scoped append-only effort/spend/refund projection, atomic reservation/settlement plus audit/freshness commit, injected-audit-failure rollback, replay de-duplication, per-attempt reserve/consume/release lifecycle, durable operation lease fencing, retry isolation, runtime refusal after reservation failure, strict/idempotent boundaries, migration-contract/PostgreSQL concurrency and immutability, stale-hold projection, approval-protected confirmed-orphan release with an immutable reason, immutable proposal preparation and owner-decision history, request-shape, backend, and Angular tests; portfolio execution and exactly-once external effects remain separate milestones |
| Owner-scoped advisory pursuit portfolio planning | `be/pursuit/portfolio_planning.go`, `be/pursuit/portfolio_capacity.go`, `be/pursuit/portfolio_calibration.go`, `be/lifeops.EvaluatePriority`, `be/resourceplanner`, `be/controlledlearning`, and authenticated `POST /api/v1/pursuits/portfolio-plan` | focused deterministic-replay, owner-isolation, 25-factor contribution, dependency-order, missing-estimate, unresolved-dependency, resource-ledger-unavailable, effort/spend-ceiling, envelope-validation, approval/risk visibility, no-mutation, handler-authentication, route-permission, optional calibration suggestion, exact applied-revision binding, stale-binding refusal, and rollback-ineligibility tests. Production wiring requires the latest durable owner-matched LifeOps snapshot, recomputes 24-hour freshness against server time, refuses missing/stale/review-required/low-confidence/unavailable state, caps merged candidate windows to confirmed minutes, and overrides browser capacity/energy factors. Controlled learning may offer an owner-approved calibration but cannot silently change explicit duration/cost, priority, risk, budget, permissions, or execution authority. Results remain `advisory_only` with `canExecute=false`; live wearable capacity feeds and external-calendar acceptance remain separate milestones. |
| Governed portfolio allocation acceptance | `be/pursuit/portfolio_acceptance.go`, `be/pursuit/portfolio_acceptance_repository.go`, migration `0038_pursuit_portfolio_allocations`, owner-only `POST /api/v1/pursuits/portfolio-plan/accept`, owner-scoped `GET /api/v1/pursuits/portfolio-allocations`, and `fe/pages/pursuits` | exact confirmation, fresh server replan, decision-digest drift rejection, canonical request binding, owner isolation, immutable plan/item digests, atomic allocation/item/reservation/activity commit, resource-ceiling rollback, concurrent exact replay, changed plan-ID conflict, append-only database guards, bounded newest-first history, history digest/schedule/status revalidation, authority-response validation, duplicate-submit prevention, lazy UI loading, retry/empty states, and Angular service/component tests. Accepted capacity is durable and survives reload; approval consumption, task enqueue, execution, and external-calendar writes remain separate governed commands. |
| Governed portfolio execution-proposal preparation | `be/pursuit/portfolio_execution_proposal.go`, `be/pursuit/portfolio_execution_proposal_repository.go`, migration `0039_pursuit_portfolio_execution_proposals`, owner-only `POST /api/v1/pursuits/portfolio-allocations/:allocationId/execution-proposals`, and `fe/pages/pursuits` | exact `PREPARE EXECUTION PROPOSALS` confirmation, expected allocation-digest binding, owner isolation, allocation/item/reservation digest verification, immutable pursuit and policy state snapshots, append-only parent/item/activity persistence, `prepared` / `prepared_needs_approval` / `prepared_blocked` derivation, exact replay for unchanged snapshots, new immutable snapshots after state changes, response-authority validation, and backend, PostgreSQL, route, service, and component tests. Every result remains `proposal_only` with `canExecute=false`; it cannot approve, consume approval, enqueue, invoke tools/runtimes, settle reservations, or execute. |
| Durable portfolio execution-proposal recovery | owner-only proposal-history and batch-coordination reads plus `fe/pages/pursuits` allocation-history restoration | Requires 1-20 unique valid allocation IDs, scopes all reads to the authenticated owner, restores only the newest immutable snapshot for each requested allocation, verifies every parent/item digest and requested-allocation boundary, preserves request order, and returns only `proposal_only` / `canExecute:false` records. One subsequent owner-scoped batch read accepts 1-20 unique proposal IDs, loads current coordination with fixed aggregate queries and a 500-item total cap, and remains `coordination_preview_only` / `canExecute:false`. Every restored proposal requires revalidation; every current coordination snapshot remains explicitly revalidation-required because dispatch independently checks exact evidence. The frontend clears stale selections first, restores decision and coordination state after reload, rejects malformed, duplicate, foreign, authority-escalating, counter-invalid, freshness-escalating, duplicate-selection, or incomplete-selection responses, and never preselects work. Full Go, route-permission, service, component, 314 Angular tests, production build, isolated PostgreSQL, and deployed browser recovery pass. |
| Governed per-item execution-proposal decisions | `be/pursuit/portfolio_execution_decision.go`, `be/pursuit/portfolio_execution_decision_repository.go`, migration `0040_pursuit_portfolio_execution_proposal_decisions`, owner-only `GET`/`POST /api/v1/pursuits/portfolio-execution-proposal-items/:itemId/decisions`, and `fe/pages/pursuits`; decisions `approved`, `rejected`, `needs_clarification`, and `revoked` use exact paired confirmations | Authenticated-owner isolation; immutable parent proposal, proposal-item, captured-state, latest-decision, and returned-history-chain verification; append-only decision and activity persistence; bounded 30-minute approval expiry; revocation linked to the latest approval; exact replay; blocked/stale/settled refusal; database update/delete rejection; response contract `authority: approval_decision_only`, `canExecute:false`; zero queue/task/pursuit/reservation/runtime/provider/external-effect mutation tests; full Go and Angular suites; disposable-PostgreSQL migration acceptance; and local authenticated browser approval, reload, revocation, and database-chain verification. Concrete execution authorization remains separate. |
| Exact portfolio workflow-effect authorization | `be/pursuit/portfolio_workflow_authorization.go`, owner-scoped repository resolver, canonical `be/executionapproval` composite plus `be/executionauth`, owner-only `POST /api/v1/pursuits/portfolio-execution-proposal-items/:itemId/authorize-workflow`, and `fe/pages/pursuits` | Fixed server-owned action/resource/tool/runtime/autonomy/risk/reversibility/cost contract; exact confirmation and item/decision digest binding; independent current owner/latest-decision/expiry/revocation/state/reservation revalidation; immutable unified-policy receipt persistence; composite resolver no-fallback behavior; malformed/foreign/stale/revoked/expired/settled/mismatched evidence refusal; response validation and UI receipt inspection; focused and full Go plus 285 Angular tests and production build. Result remains `execution_authorization_only`, `canExecute:false`; receipt consumption, workflow creation, tool/runtime invocation, settlement, and external effects remain separate. |
| Single-use portfolio workflow creation | `be/pursuit/portfolio_workflow_authorization.go`, canonical `be/executionauth` consumption ledger, idempotent `be/workflow` intake, migrations `0042_workflow_active_source_idempotency` and `0043_pursuit_activity_idempotency`, owner-only `POST /api/v1/pursuits/portfolio-execution-proposal-items/:itemId/execute-workflow`, and `fe/pages/pursuits` | Separate exact `CREATE APPROVED PORTFOLIO WORKFLOW` confirmation; receipt/owner/item/decision/effect binding; current approval on first use; exact consumer and target consumption; recoverable replay after interruption; owner/source duplicate prevention; direct linked-workflow replay without repeated intake; independently recoverable link and final audit; deterministic concurrent audit de-duplication without rewriting history; review-gated local workflow; zero reservation settlement or downstream execution; mismatched or differently consumed receipts fail closed; full Go, 287 Angular tests, production build, and local API/browser replay acceptance. Result is `workflow_effect_executed`, `canExecute:false`; workflow review/run, provider effects, and reservation settlement remain separate. |
| Governed portfolio dispatch coordination | portfolio dispatch service/repository, migration `0045`, owner-only single and bounded batch coordination reads, owner-only dispatch command, and `fe/pages/pursuits` | Batch recovery accepts 1-20 unique proposal IDs, preserves request order, uses owner-scoped fixed aggregate reads, caps combined items at 500, rejects 21 IDs before storage access, and fails all-or-nothing without leaking which proposal was unavailable. Explicit dispatch requires selection of 1-20 eligible items, exact `DISPATCH APPROVED PORTFOLIO WORKFLOWS` confirmation, immutable proposal/item/decision/digest binding, exact selected-item set equality, per-item current-approval and policy revalidation, exact effect authorization, single-use workflow creation, append-only run/item attempt ledgers, exact retry and partial-resume behavior, owner/actor/digest/selection isolation, strict response-counter validation, and independent per-workflow verification loading. The full Go suite, authenticated route-permission tests, 314 Angular tests, production build, isolated PostgreSQL aggregate-query test, deployed browser recovery, live migration `0045`, append-only trigger probes, and invalid-provenance trigger probes pass. The coordinator never creates approval decisions, never runs workflows, never settles reservations, and never invokes external providers; successful items create only review-gated local workflows and all responses retain `canExecute:false`. |
| Exact reviewed workflow execution | `be/workflow.RunOneForOwner`, owner-only `POST /api/v1/workflow/:id/run`, atomic repository claim, existing `be/workflowtask` runner, and `fe/pages/workflow-engine` | Owner and exact-ID isolation, ready/approved/due claim, duplicate-run skip, emergency-stop preservation, downstream task/runtime authorization and verification reuse, actual result visibility, confirmation dialog, focused Go handler/service/router tests, 289 Angular tests, and strict local Playwright acceptance. This is a precise local workflow command; live mutable-provider acceptance remains separate. |
| Internal reminder proposal projection | `be/workflow/reminder_proposal.go`, authenticated read-only `GET /api/v1/workflow/reminder-proposals`, and `fe/pages/workflow-engine` | Bounded horizon/limit validation, owner-scoped PostgreSQL join, open-checklist and active-workflow filtering, exact checklist/workflow identity, deterministic time ordering, duplicate refusal, due/upcoming counter validation, strict `reminder_proposal_only` and `canExecute:false` authority, current freshness with mandatory revalidation, fail-closed UI refresh, route smoke/read-permission tests, focused Go tests, real PostgreSQL owner-isolation test, 324 Angular tests, and production build. No workflow, approval, notification, follow-up, provider, runtime, or Calendar mutation is permitted. |
| Governed reminder preparation and decision evidence | `be/models/workflow_reminder_activation.go`, `be/workflow/reminder_activation.go`, `be/workflow/reminder_activation_repository.go`, migrations `0046_workflow_reminder_activation_ledger` and `0047_workflow_reminder_activation_decision_order`, authenticated `POST /api/v1/workflow/reminder-proposals/:itemId/activation-requests`, `GET /api/v1/workflow/reminder-activation-requests`, and authenticated `GET` plus approval-protected `POST /api/v1/workflow/reminder-activation-requests/:requestId/decisions` | Exact current reminder-digest and `internal_notification` binding; authenticated owner equals recorded actor; owner/idempotency-key replay; immutable request and decision record digests; 15-minute request validity; chained concurrent decision-tip protection; strict monotonic `decided_at` advancement beyond the current chain tip; 10-minute approval validity; exact decision-specific confirmation; owner-scoped bounded history; stale source refusal; and database insert validation plus update/delete/truncate rejection. The process-local router `Idempotency-Key` cache explicitly defers these mutation routes to the durable owner-scoped ledger: preparation replays by owner/body idempotency key and request digest, while decisions replay by owner/request/canonical request digest and verify the current chain tip. Migration-chain contract, isolated PostgreSQL `0046`+`0047` ledger test, live repository PostgreSQL test, full Go suite, 324 Angular tests, production build, and signed-in browser prepare/approve/persist/cleanup acceptance pass. All contracts return `canExecute:false`. Preparation and approval are evidence only and cannot create Calendar events, notifications, email or other messages, call providers/runtimes, execute follow-ups, mutate workflow/checklist state, or authorize an external effect. No reminder worker is active, and the existing open-loop subsystem is separate. |
| Verified portfolio reservation settlement | `be/pursuit/portfolio_workflow_settlement.go`, `be/pursuit/controlled_learning.go`, `be/controlledlearning/estimate_calibration.go`, append-only resource ledger, owner-only `POST /api/v1/pursuits/portfolio-execution-proposal-items/:itemId/settle-workflow`, `fe/pages/pursuits`, and Governance Control | Exact item/workflow/confirmation binding; owner isolation; original approved decision, authorization receipt, single-use consumption target, pursuit link, terminal workflow, task-plan identity, and accepted verification revalidation; internally derived evidence URI; measured effort/cost accounting; exact replay; changed-outcome, incomplete, unverified, foreign, unlinked, and receipt-mismatch refusal. A successful commit emits one idempotent controlled-learning outcome with proof, attestation, receipt, and estimated-versus-actual resource evidence. Three comparable verified outcomes can create one deterministic review-required calibration proposal; minimum-sample, scope, robust-median, evidence-digest, explicit-approval, exact-revision, stable-history, rollback, post-review cohort, rejection-anchor, pending-review suppression, and material-drift tests cover the learning loop. A learning outage is reported without invalidating accounting; replay backfills evidence without repeated work. Result is `verified_accounting_only`, `canExecute:false`; settlement and unapproved evidence grant no provider/runtime authority and cannot change policy, permissions, safety, or memory. |
| Workflow state | `be/workflow`, `statemachine` | `workflow/*_test.go`, `statemachine` |
| Audit | `be/events`, `auditevent` | `auditevent` |

## Cross-cutting requirements

| Requirement | Home | Evidence |
| --- | --- | --- |
| No false completion | honest matrix + `stabilization-gates` | rows cite evidence |
| Bounded/rate-limited external actions | `ratelimit`, `idempotency`, `backoff` | package tests |
| Safety boundary (no unrestricted autonomy) | `autonomygate`, `actionresolver`, emergency stop | package tests |
| Grounded verification | `be/verification` distinguishes exact connected-source provenance, source support, deterministic verification, human approval, conflict, and unsupported states; duplicate retrieval hits are collapsed and connected-source payloads must match the durable raw/extraction snapshot | `verification/authority_test.go` proves caller-forged authority is rejected, trusted external evidence remains only source-supported, exact connected provenance is source-supported rather than truth-verified, and missing provenance resolution fails closed |
| Semantic claim state | immutable temporal claims are assessed by owner/workspace, subject/predicate, effective time, observed time, independent provenance, conflict, and supersession; authority text is ignored and a complete bounded read is required | `knowledgegraph/claim_assessment_test.go`, `claim_handler_test.go`, and `router/knowledge_claim_permissions_test.go`; assessment remains advisory and grants no execution authority |
| Privileged claim-state boundary | generic claim creation rejects verification and approval states; human correction requires `approve`, appends an immutable successor and durable local reason source, is idempotent by request ID, and does not convert human approval into external source support | `knowledgegraph/claim_handler_test.go`, `router/knowledge_claim_permissions_test.go`, `knowledge-claims.component.spec.ts`, and authenticated local review-queue acceptance |
| Fail-closed immutable framework provenance | selector-v5 requests must independently resolve the exact owner/selection record and match IDs, versions, risk/autonomy/approval contracts, and four governance digests; the same comparison runs again before receipt consumption | `executionauth/framework_selection_test.go`; denied receipts use `framework.selection_unverified`, while pre-consumption drift returns `ErrAuthorizationChanged` |
| Framework evidence before effects | selected evidence requirements compile to deterministic, source-attributed, required contracts; applicable `pre_authorization` assertions are evaluated before execution and missing evidence produces a blocked review result with no external effect | `task/framework_evidence.go`, `task/service.go`; current deterministic validators cover HAI-held governance/context records, not absent real-world providers |
| Completion-first model calibration | each real routed generation that reaches a provider/model writes a redacted `model_run_telemetries` attempt; task validation reconciles the exact telemetry ID with its trusted outcome and method; calibration reloads from the durable ledger; and route selection ranks accepted-output evidence conservatively by lane before cost, token, latency, or local-first tie-breakers | `llm/calibration_test.go`, `modelintelligence/calibration_test.go`, `telemetry_persist_test.go`, `task/service.go`, migration `0031`, and the LLM Policy/Model Intelligence frontend contracts. Direct generation API calls remain `unvalidated` until a trusted validator records an outcome. External-provider acceptance remains gated on real credentials, live provider execution, and retained provider-specific evidence. |
| Source-evidence trust boundary | exact claim resolution proves bounded raw-item provenance, integrity, and freshness from `fetched_at`; it does not prove semantic authority or truth, and source-wide `LastSyncedAt` cannot make an older raw item fresh | authorization inspection returns only verified source-claim counts and digest fingerprints; source claims, URIs, extracted payloads, and raw sensitive content are not exposed |
| Execution and postcondition evidence phases | action/runtime/provenance requirements are tagged `execution`; verification/result/deliverable requirements are tagged `postcondition` | typed requirements use exact phase-specific evaluators and never the fuzzy historical matcher; live provider-specific assertion producers and acceptance evidence remain separate readiness gates |
| Authenticated outcome scope | outcome handler replaces body owner/workspace/outcome scope with authenticated owner and URL scope before persistence | `outcomeevaluation/handler_api_test.go`; local revision/evaluation rows retained owner scope and non-empty digests |
| Advisory outcome monitoring | immutable definitions, source-attributed observations, bounded evaluation, and correction history are exposed in Governance Control without granting execution authority | component/service tests; disposable-PostgreSQL collector/repository/composition lifecycles; 2026-08-03 browser acceptance produced revision 2 and an `on_track` evaluation; 2026-08-14 signed no-op pass retained exact 10/10/10 ledger counts and reported no execution or delivery |
| Owner-governed proactive attention | immutable owner-scoped feedback binds the latest advisory decision and supports accept, exact-revision dismiss, bounded snooze, indefinite suppress, and resume; current controls are applied before cooldown and interruption-budget evaluation and never grant delivery or execution authority | `proactivity/feedback_test.go`, handler and router permission tests, Governance Control component/service tests, migration `0048` contract and isolated PostgreSQL lifecycle test |
| Outcome-to-life context | outcome revisions and evaluations project idempotent, owner-scoped graph nodes and workspace/revision relations; projection failures are redacted and cannot roll back the outcome ledger | `outcomeevaluation/life_ontology_projection_test.go`; 2026-08-03 local-stack acceptance produced revision 2, an `on_track` evaluation, two outcome nodes, and three relations |
| Explicit whole-life classification | new outcome revisions require a supported life domain; old revisions remain readable with a review-marked fallback | `outcomeevaluation/validation_test.go`, `outcomeevaluation/life_ontology_projection_test.go`; Governance Control component tests |
| Authorization-to-life context | immutable authorization receipts retain domain and project advisory outcomes, exact approval evidence, commitment context, and non-incurred estimated costs without granting authority | `executionauth/life_ontology_projection_test.go`, `executionauth/repository_postgres_test.go`, migration `0025` contract and PostgreSQL tests |
| Authenticated commitment scope | life-ledger handlers take owner identity from the authenticated session; commitment keys, history, linked costs, and lists remain owner-isolated | `lifeledger/service_test.go`; authenticated route registration in `router/routes.go`; disposable-PostgreSQL suite and local authenticated UI acceptance |
| Immutable commitment lifecycle | every change appends an expected-revision-checked record; terminal states cannot be reopened and database update/delete/truncate are rejected | `lifeledger/service_test.go`; migration `0026` contract tests |
| Distinct cost semantics | estimate, incurred, paid, and refund are separate immutable events with positive minor-unit amounts, currency, evidence, and optional same-owner commitment/project links; incurred requires source support and paid/refund require human-confirmed or verified evidence | backend validation/handler tests, component refusal tests, local browser refusal acceptance; no external balance or payment reconciliation is claimed |
| Commitment/cost-to-life context | accepted records project owner-scoped commitment and cost nodes plus project and incurs-cost relations; projection remains advisory and failure cannot rewrite the ledger | `lifeledger/service_test.go`, `lifeledger/projection.go`; full disposable-PostgreSQL suite and authenticated UI inspection/authoring acceptance |
| Standing-mandate-to-life context | each persisted draft/active/revoked revision projects a restricted, local-only outcome with source provenance and bounded project links; projection is excluded from the mandate digest and grants no authority | `standingmandate/life_ontology_projection_test.go`, `standingmandate/life_ontology_projection.go` |
| Standing-mandate authority continuity | callers may supply only one explicit mandate UUID; authenticated owner scope is enforced at every durable boundary, candidate acceptance preserves the same mandate, workflow/task/automation handoffs cannot substitute it, and final authorization binds immutable request/mandate/decision/approval digests before repeating the comparison immediately before consumption | `assistant/service_test.go`, `pursuit/service_test.go`, `workflow/service_test.go`, `workflowtask/runner_test.go`, `task/*approval*test.go`, `automation/*approval*test.go`, `executionauth/*test.go`, migration `0032` contract/PostgreSQL tests, and Task Blueprint component tests. No implicit mandate selection or authority from a reference alone is permitted. |
| Cautious contact discovery | contact-bearing sources can project deduplicated person candidates, but candidate confidence, verification, sensitivity, and local-only boundaries remain stricter than the source document | `source/life_ontology_projection_test.go`, `lifeontology/service_test.go` |
| Human-governed contact promotion | owner review appends an immutable promote/correct/reject/merge/keep-distinct decision; source candidates remain unchanged; canonical people are separate local-only `human_approved` records and never grant execution authority | `lifeontology/contact_review_test.go`, `lifeontology/handler_test.go`, `lifeontology/repository_postgres_integration_test.go`, migration `0027` contract/PostgreSQL tests, Governance Control component/service tests |
| Contact-review transaction safety | same-owner candidate/proposal foreign keys, proposal-membership validation, one final decision per subject, server time, secret rejection, and atomic canonical-person plus decision persistence fail closed under concurrent decisions | disposable PostgreSQL `TestPostgresContactReview*` and `TestContactReviewDecisionMigrationRollbackSafety`; full Go suite |
| Read-only Google contact intake | the People API connector requests only `contacts.readonly`, performs bounded backfill and native sync-token updates, preserves removals as reviewable tombstones, and projects people only as non-authoritative review candidates | `googleoauth/people_test.go`, `source/contacts_test.go`, `source/oauth_test.go`, and Connected Sources component tests; a real sandbox-address-book acceptance run remains required |
| Read-only Google Calendar intake and governed planning | the Calendar API connector requests only `calendar.readonly`, reads only primary-calendar events through GET, performs a bounded one-year backfill and native sync-token updates, retains event timing for deterministic due dates, creates preparation proposals only for meaningful events within 14 days, keeps past events context-only, detects changed-event overlaps within a 30-day horizon, and retracts stale conflict/event work when source timing changes or is cancelled without deleting obligations or writing back | `googleoauth/calendar_test.go`, `source/calendar_test.go`, `source/service_test.go`, `source/oauth_test.go`, `workflow/service_test.go`, and Connected Sources component tests; a real sandbox-calendar acceptance run remains required |
| Portfolio-plan authority boundary | portfolio planning reads owner-visible pursuits and resource-ledger state, computes a pure 25-factor ranking, and deterministically proposes dependency-ordered capacity allocations; the planning route cannot persist priority, mutate pursuit state, reserve resources, consume approval, enqueue execution, or write calendar events. A separate owner-only acceptance command may append exact resource holds but remains `allocation_only`. The execution-proposal command snapshots current item state but remains `proposal_only`, and its bounded history route only restores immutable snapshots after reload. Recovered snapshots explicitly require current eligibility revalidation. The owner-scoped batch coordination read restores only current read evidence and retains `canExecute:false`. The per-item decision command appends owner decision evidence as `approval_decision_only`. An unexpired latest approval may be resolved only for one server-fixed reversible workflow-intake effect and yields an unconsumed `execution_authorization_only` receipt. A second owner command consumes that exact receipt once and creates one review-gated local workflow, returning `workflow_effect_executed` with `canExecute:false`. The batch coordinator composes only those same authorization and workflow-creation boundaries for explicitly selected approved items; it cannot create approvals, run workflows, settle reservations, or invoke external effects. Exact reviewed execution remains separately gated. Only a `verified` or `test_passed` worker completion receives an immutable attestation; a later owner command may account measured use only after atomically revalidating the exact receipt, consumption, single workflow link, attestation, and reservation chain. | Planning, acceptance, proposal preparation/recovery, batch coordination recovery, per-item decision, exact-effect authorization, idempotent workflow creation, governed dispatch, exact reviewed execution, post-verification settlement, and controlled reconciliation-evidence boundaries are covered by focused service/repository/handler tests, authenticated route-permission tests, composite-resolver tests, migration contracts through `0045`, all 314 Angular tests, production build, isolated PostgreSQL query verification, deployed browser acceptance, retained-volume migration verification, and live append-only/provenance trigger probes. Calendar and mutable external-provider effects remain open and separately approval-gated. |
| Complex Trello-card context | `be/source/trello.go` imports description, chronological comments, checklists, and attachment metadata with the card URL as provenance; attachment bodies remain explicit source reads | `be/source/trello_test.go`; fresh live complex-card acceptance remains required |
| Fixed advisory outcome collectors | Monitor targets select only `workflow_open_loop_count`, `workflow_verified_completion_count`, or `overdue_commitment_count`; collectors issue fixed owner-scoped local-ledger queries and accept no caller SQL, URL, script, expression, provider command, or arbitrary tool input | `ambientmonitor/collectors_test.go`, `ambientmonitor/security_test.go`; required acceptance must also compare each count and source digest with the target database snapshot |
| Durable immutable monitor evidence | Each completed collection appends an owner/workspace/outcome/indicator-scoped observation and lease-fenced run receipt; exact completion replay reuses the stored collection evidence and changed input conflicts; update/delete/truncate of evidence ledgers is rejected. Collection completion is not composition success. | `ambientmonitor/service_test.go`, `ambientmonitor/repository_postgres_test.go`, migration `0049` contract and PostgreSQL lifecycle tests; parent release verification remains required before claiming the complete suite green |
| Durable advisory composition handoff | Each successful run has one owner/workspace-scoped `0050` delivery bound to immutable run/observation identities and digests. Claims are revision- and lease-generation-fenced; transient failures remain `pending`, bounded exhaustion becomes `dead_lettered`, success becomes `succeeded`, and every result appends an immutable attempt receipt. Existing successful runs are backfilled idempotently. | migration `0050` contract and disposable-PostgreSQL lifecycle tests cover backfill, retry, success, dead-lettering, stale fencing, immutability, owner isolation, and rollback guards; scheduler/API/UI and crash-boundary acceptance remain open |
| Advisory outcome and attention composition | A successful delivery attempt maps immutable monitor evidence to source-supported outcome evidence and passes it idempotently to the proactivity decision/inbox path. Failed or dead-lettered composition does not rewrite the completed observation/run. Exact outcome-definition, proactivity policy/feedback, and composer-version snapshots are not yet pinned, so exact historical replay is an explicit gap. | `ambientmonitor/composer_test.go` and composition queue service/repository tests; required acceptance must prove one downstream advisory result without duplicate on retry and record which advisory snapshot was used |
| Monitor authority boundary | Every target, observation, run, composition delivery, attempt receipt, completion, and API response declares `advisory_monitor_only`; execute, deliver, notify, Calendar-write, workflow-mutation, mandate-authorization, external-effect, and learning-mutation capabilities remain false | migration `0050` constraints, `ambientmonitor/security_test.go`, handler/router permission tests, and Governance Control component/service tests; required acceptance must observe zero downstream effect records and zero external calls |
| Durable monitor scheduling and recovery | A persisted singleton `outcome-monitor.sweep` discovers bounded due collection and composition owner/workspace scopes, recovers expired leases, processes bounded batches, and obeys background safety gating | `ambientmonitor/durable_scheduler_test.go`; required restart, idempotent replay, disable, active-lease fencing, expired-lease recovery, dead-letter, and two-owner isolation acceptance remains release evidence |
| Guarded monitor operator surface | Authenticated owner and recognized-role guards protect all monitor routes; read, write, and administrator permissions are separated, server-bound owner identity is never accepted from JSON, and Governance Control exposes configuration, pause/resume, bounded run, and immutable history | `ambientmonitor/handler_test.go`, router permission tests, Angular monitor service/component tests; signed-in browser acceptance remains required |

## Coverage summary

Every critical-path link has a repository implementation and at least focused
contract coverage. This is not production-readiness evidence. Open traceability
gaps include exact outcome/proactivity snapshot pinning for delayed composition,
retained live provider reconciliation for Contacts and life ledgers,
standing-mandate acceptance by configured external runtimes, independent exact
assertion producers for every execution and
postcondition evidence contract, and broader end-to-end cross-link tests that
require a live stack.
